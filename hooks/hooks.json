{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/session-start-context.py\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "whoop_get_latest_recovery|whoop_get_latest_sleep|whoop_get_latest_strain|whoop_get_strain_range|whoop_get_training_summary|whoop_get_recovery_range|whoop_get_sleep_range|whoop_get_workouts",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before calling this Whoop tool: read today's entry in session-log.md. If whoop_pulled is 'yes', do NOT proceed — use cached context instead and skip this call. Only proceed if whoop_pulled is 'no' or the field is absent from today's entry."
          }
        ]
      },
      {
        "matcher": "get-recent-activities|get-all-activities|get-activity-details",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before calling this Strava tool: check strava_last_pull in today's session-log.md. If it was pulled within the last 4 hours AND the user has not reported completing an activity since then, skip this call and use session log context instead."
          }
        ]
      }
    ]
  }
}
